/// Layout metrics shared across the app.
abstract final class AppMetrics {
  /// Height of the top app bar.
  static const appBarHeight = 52.0;

  /// Height of the mini player.
  static const miniPlayerHeight = 70.0;

  /// Horizontal margin of the mini player.
  static const miniPlayerHorizontalMargin = 20.0;

  /// Height of the bottom navigation bar.
  static const navigationBarHeight = 62.0;
}
